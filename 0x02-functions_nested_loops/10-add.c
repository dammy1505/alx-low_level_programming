#include "main.h"
#include <unistd.h>

/**
 * add - function to add two integers and print the answer
 * @a: a is the left operand
 * @b: b is the right operand
 * Return: returns the result which is the summation of a and b
 */

int add(int a, int b)
{
	return (a + b);
}
