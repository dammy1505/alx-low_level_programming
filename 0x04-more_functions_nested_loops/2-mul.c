#include "main.h"
#include <unistd.h>

/**
 * mul - function to multiply two integers
 * @a: the left operand
 * @b: the right operand
 * Return: returns the multiplication of a and b
 */

int mul(int a, int b)
{
	return (a * b);
}
