#include <stdio.h>

/**
 * main - prints the phrase as indicated in main
 * Return: 0 if exited properly, otherwise non zero
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
